---
import BlogPost from "../../components/BlogPost.astro";
import Layout from "../../layouts/Layout.astro";

// Esta función
export const getStaticPaths = async () => {
  const allPosts = await Astro.glob("../posts/*.md");

  const uniqueTags = [
    ...new Set(allPosts.map((post) => post.frontmatter.tags).flat()),
  ];

  return uniqueTags.map((tag) => {
    const filteredPosts = allPosts.filter((post) =>
      post.frontmatter.tags.includes(tag)
    );

    return {
      params: { tag },
      props: { posts: filteredPosts },
    };
  });
};

const { tag } = Astro.params;
const { posts } = Astro.props;
---

<Layout title={tag}>
  <h1 class="p-20 font-bold text-3xl">
    Post con la etiqueta <span class="text-blue-500">{tag}</span>
  </h1>

  <ul class="flex flex-col w-full justify-center items-center p-5 gap-5">
    {
      posts.map((post) => {
        const { title, pubDate: date, description, url } = post.frontmatter;

        return (
          <BlogPost
            title={title}
            date={date}
            description={description}
            url={url}
            date={new Date(date).toLocaleDateString()}
          />
        );
      })
    }
  </ul>
</Layout>
